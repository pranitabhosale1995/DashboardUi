import React from "react";
import {
  Box,
  Typography,
  Stack,
  Chip,
  IconButton,
  Tooltip,
  Divider,
} from "@mui/material";

import RefreshRoundedIcon from "@mui/icons-material/RefreshRounded";
import AccountBalanceRoundedIcon from "@mui/icons-material/AccountBalanceRounded";
import AccessTimeRoundedIcon from "@mui/icons-material/AccessTimeRounded";
import WifiRoundedIcon from "@mui/icons-material/WifiRounded";

import dashboardData from "../dashboard.json";

const DashboardHead = () => {
  const lastUpdated = dashboardData?.meta?.lastUpdated;

  const formattedDate = lastUpdated
    ? new Date(lastUpdated).toLocaleString("en-IN", {
        day: "2-digit",
        month: "short",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit",
      })
    : "N/A";

  const handleRefresh = () => {
    window.location.reload();
  };

  return (
    <Box
      sx={{
        background:
          "linear-gradient(135deg, #ffffff 0%, #f8faff 100%)",
        border: "1px solid #e4e7ec",
        borderRadius: 3,
        px: { xs: 2, md: 3 },
        py: 2,
        boxShadow: "0 4px 18px rgba(16, 24, 40, 0.05)",
      }}
    >
      {/* TOP ROW */}
      <Box
        sx={{
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          gap: 2,
          flexWrap: "wrap",
        }}
      >
        {/* LEFT */}
        <Stack
          direction="row"
          spacing={1.5}
          alignItems="center"
        >
          <Box
            sx={{
              width: 46,
              height: 46,
              borderRadius: 2.5,
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              background: "#f1f5ff",
              border: "1px solid #dbe4ff",
            }}
          >
            <AccountBalanceRoundedIcon
              sx={{
                fontSize: 26,
                color: "#1d4ed8",
              }}
            />
          </Box>

          <Box>
            <Typography
              sx={{
                fontSize: { xs: 19, md: 23 },
                fontWeight: 800,
                lineHeight: 1.2,
                color: "#101828",
              }}
            >
              SBI Performance Dashboard
            </Typography>

            <Typography
              sx={{
                mt: 0.4,
                fontSize: 12,
                color: "#667085",
                fontWeight: 500,
              }}
            >
              Banking Operations & Production Analytics
            </Typography>
          </Box>
        </Stack>

        {/* RIGHT */}
        <Stack
          direction="row"
          spacing={1}
          alignItems="center"
          flexWrap="wrap"
        >
          <Chip
            icon={<WifiRoundedIcon />}
            label="Live Data"
            size="small"
            sx={{
              fontWeight: 700,
              background: "#ecfdf3",
              color: "#027a48",
              "& .MuiChip-icon": {
                color: "#12b76a",
                fontSize: 17,
              },
            }}
          />

          <Tooltip title="Refresh Dashboard">
            <IconButton
              onClick={handleRefresh}
              size="small"
              sx={{
                border: "1px solid #d0d5dd",
                background: "#fff",
              }}
            >
              <RefreshRoundedIcon fontSize="small" />
            </IconButton>
          </Tooltip>
        </Stack>
      </Box>

      <Divider sx={{ my: 2 }} />

      {/* STATUS ROW */}
      <Box
        sx={{
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          gap: 2,
          flexWrap: "wrap",
        }}
      >
        <Stack
          direction="row"
          spacing={1}
          alignItems="center"
          flexWrap="wrap"
        >
          <Chip
            label="SBI"
            size="small"
            sx={{
              fontWeight: 800,
              background: "#eef4ff",
              color: "#175cd3",
            }}
          />

          <Chip
            label="Production Analytics"
            size="small"
            variant="outlined"
            sx={{
              fontWeight: 600,
              color: "#344054",
              borderColor: "#d0d5dd",
            }}
          />

          <Chip
            label="Circle → Region → Branch"
            size="small"
            variant="outlined"
            sx={{
              fontWeight: 600,
              color: "#475467",
              borderColor: "#d0d5dd",
            }}
          />
        </Stack>

        {/* LAST UPDATED */}
        <Stack
          direction="row"
          spacing={0.7}
          alignItems="center"
        >
          <AccessTimeRoundedIcon
            sx={{
              fontSize: 16,
              color: "#667085",
            }}
          />

          <Typography
            sx={{
              fontSize: 11.5,
              color: "#667085",
              fontWeight: 500,
            }}
          >
            Last updated: {formattedDate}
          </Typography>
        </Stack>
      </Box>
    </Box>
  );
};

export default DashboardHead;